async function run() { try { const res = await fetch('http://localhost:3000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'admin@codielead.com', password: 'password123' }) }); const data = await res.json(); console.log('Login status:', res.status, data); if (data.accessToken) { const statsRes = await fetch('http://localhost:3000/api/superadmin/stats', { headers: { 'Authorization': 'Bearer ' + data.accessToken } }); console.log('Stats status:', statsRes.status, await statsRes.json()); } } catch (e) { console.error(e); } } run();
